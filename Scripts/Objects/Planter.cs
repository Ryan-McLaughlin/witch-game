using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Planter : MonoBehaviour
{
    // dirt level
    // dirt quality

    // water % 

    // size

    // plants

    // update plants
    // track space usage
    // updates dirt
    // updates water - general evaporation, not plant usage (plants take water from planter)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
